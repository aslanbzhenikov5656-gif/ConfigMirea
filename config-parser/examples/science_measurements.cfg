{{!--
Результаты научных измерений
--}}

const r_one = 9;
const r_two = 16;
const r_three = 25;

<<
  q(measurements),
  <<
    q(sample),
    $1$,
    q(radius),
    $r_one$,
    q(root),
    $sqrt(r_one)$
  >>,
  <<
    q(sample),
    $2$,
    q(radius),
    $r_two$,
    q(root),
    $sqrt(r_two)$
  >>,
  <<
    q(sample),
    $3$,
    q(radius),
    $r_three$,
    q(root),
    $sqrt(r_three)$
  >>,
  <<
    q(summary),
    <<
      $sqrt(r_one)$,
      $sqrt(r_two)$,
      $sqrt(r_three)$
    >>
  >>
>>;
